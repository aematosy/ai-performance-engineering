# Template for local-only runtime properties.
# Safe to commit because it contains no secret values.
# Copy this template locally and fill the required values before execution.
secret_auth_createtoken_password=
