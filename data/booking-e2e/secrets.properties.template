# Template for local-only JMeter properties.
# Safe to commit because it contains no secret values.
# Copy this file locally and fill the required values before execution.

secret_auth_createtoken_password=
